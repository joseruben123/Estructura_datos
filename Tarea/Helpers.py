Ayudante de clase:
    def __init__(self):
        x=10
        pasar
    menú def (self,opciones,titulo):
        print(titulo)
        para  opciones en opciones:
            print(opcion)
        opc = input("Escoja la  Opcion[1...{}]: ". format(len(opciones))))
        volver opc